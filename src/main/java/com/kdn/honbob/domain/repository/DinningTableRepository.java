package com.kdn.honbob.domain.repository;

import com.kdn.honbob.domain.model.DinningTable;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DinningTableRepository extends JpaRepository<DinningTable, Long> {
}
